---
title: "Test Style"
author: "Xiaochi"
date: "09/01/2020"
output: 
  word_document:
    reference_docx: "../common/word-style-Arial.docx"
bibliography: "../common/Broken-Hill.bib"
csl: "../common/american-geophysical-union.csl"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
	echo = TRUE,
	message = TRUE,
	warning = TRUE
)
library(tidyverse)
library(dplyr)
library(knitr)
library(pander)
panderOptions('table.alignment.default', function(df)
    ifelse(sapply(df, is.numeric), 'right', 'left'))
panderOptions('table.split.table', Inf)
panderOptions('big.mark', ",")
panderOptions('keep.trailing.zeros', TRUE)
```

## Heading 2

### Heading 3

#### Heading 4

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

# Make a table

Create a subset of the mtcars data [@Kristensen2016].

@Kristensen2015 says blah [-@Kristensen2015].

```{r message=FALSE, echo=FALSE}
# extract a subset of the mtcars data
df <- mtcars %>%
    mutate(make_model = row.names(mtcars)) %>%
    filter(cyl == 4) %>%    
    select(make_model, mpg, wt) %>%
    mutate(wt = wt*1000) %>%
    arrange(make_model) 

kable(df)
```

```{r}
pander(df)
```

```{r}
citation("ggplot2")
```




## References